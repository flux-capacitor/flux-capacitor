{
  "name": "sample-notes",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend/ && npm run build",
    "start": "cd server/ && npm start",
    "test": "cd server/ && npm test && cd ../frontend && npm test",
    "postinstall": "cd frontend/ && npm install && cd ../server && npm install"
  }
}
