{
  "name": "flux-capacitor-sample-notes",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "cd frontend/ && npm run build",
    "start": "cd server/ && npm start",
    "test": "cd server/ && npm install && npm test && cd ../frontend && npm install && npm test",
    "postinstall": "cd frontend/ && npm install && cd ../server && npm install"
  },
  "devDependencies": {
    "nothing-to-see-here": "^1.0.1"
  }
}
